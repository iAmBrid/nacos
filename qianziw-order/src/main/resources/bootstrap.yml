spring:
  application:
    name: qianziw-order
  cloud:
    nacos:
      config:
        enable: false
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        file-extension: yml
      discovery:
        server-addr: 127.0.0.1:8848

logging:
  level:
    com.qianziw.feignService.MemberFeignService: debug
